class FavoriteMovie {
  final String id;
  final String title;
  final String backdropPath; // Add other necessary fields

  FavoriteMovie({
    required this.id,
    required this.title,
    required this.backdropPath,
    // Initialize other fields as needed
  });
}
